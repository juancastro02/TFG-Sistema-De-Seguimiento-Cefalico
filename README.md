## Setup

1. Install dependencies:
   ```bash
   npm install
   npm run setup
   ```

2. Create `.env` file:
   ```bash
   cp .env.example .env
   # Edit .env and add your OpenAI API key
   ```

3. Download Whisper model (if not included):
   ```bash
   # Download base model to models/
   # See setup-voice.sh for details
   ```